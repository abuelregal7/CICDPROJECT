# .circleci/config.yml
version: 2.1
jobs:
  build:
    macos:
      xcode: "15.2.0"
    environment:
      RUBY_VERSION: "2.7.4"
      MATCH_PASSWORD: CICDPROJECT
      MATCH_GIT_BASIC_AUTHORIZATION: YWJ1ZWxyZWdhbDc6Z2hwX3BDcnVXWm11a1V2VHVLakhPTDFPNDg0bjJVbFpmZjBub3d2Z24=
    steps:
      - checkout

      - run:
          name: Install rbenv and Ruby 2.7.4
          command: |
            brew update
            brew install rbenv
            eval "$(rbenv init -)"
            rbenv install -s 2.7.4
            rbenv global 2.7.4
            rbenv rehash
            echo 'eval "$(rbenv init -)"' >> ~/.bash_profile
            echo 'rbenv global 2.7.4' >> ~/.bash_profile
            source ~/.bash_profile

      - run:
          name: Verify Ruby version
          command: ruby -v

      - run:
          name: Install Bundler
          command: gem install bundler -v 1.17.2

      - run:
          name: Install dependencies
          command: |
            bundle install
            gem install cocoapods

      - run:
          name: Install CocoaPods dependencies
          command: bundle exec pod install

      - run:
          name: Install coreutils
          command: brew install coreutils

      - run:
          name: Debug Environment Variables
          command: |
            echo "MATCH_PASSWORD: CICDPROJECT"
            echo "MATCH_GIT_BASIC_AUTHORIZATION: YWJ1ZWxyZWdhbDc6Z2hwX3BDcnVXWm11a1V2VHVLakhPTDFPNDg0bjJVbFpmZjBub3d2Z24="

      - run:
          name: Run Fastlane to Build the App
          command: |
            bundle exec fastlane buildApp_localy
            

workflows:
  version: 2
  build_and_test:
    jobs:
      - build



#bundle _1.17.2_ install --jobs 4 --retry 3

# # .circleci/config.yml
# version: 2.1
# jobs:
#   try-job:
#     macos:
#       xcode: 15.2.0
#     steps:
#       - run: echo "i am a job named try job within a workflow named try-workflow"

# workflows:
#   try-workflow:
#     jobs:
#       - try-job